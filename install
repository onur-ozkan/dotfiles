#! /bin/bash

# Create directories
mkdir -p ~/.config
mkdir -p ~/.dwm
mkdir -p ~/.local/bin/statusbar

# zsh / bash
cp .zshenv ~
cp .zshrc ~

# libinput
sudo cp -f ./libinput/40-libinput.conf /usr/share/X11/xorg.conf.d/40-libinput.conf

# vim-plug for nvim
sh -c 'curl -fLo "${XDG_DATA_HOME:-$HOME/.local/share}"/nvim/site/autoload/plug.vim --create-dirs \
       https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim'
nvim -c PlugInstall -c qa

# x
cp .xinitrc ~

# tmux
cp .tmux.conf ~

# dwm
cp .dwm/* ~/.dwm

# scripts
cp -r .local/bin/* ~/.local/bin

# config
cp -r .config/* ~/.config
sudo cp -r /etc ~/etc/doas.conf

# wallpapers
cp -R .backgrounds ~
